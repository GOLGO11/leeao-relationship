《蒋介石研究续集》round3：低频端点与关系质量复核
日期：2026-06-26

范围
- 承接 `notes/514-蒋介石研究续集-round2-人物关系边补网.txt`。
- 本轮目标：复核正式回流后的低频端点、别名归并、关系边权重层级，并少量补入真假保密局二级关系。

低频端点处理
- 清理本书误入端点：`金圆券`、`关键`、`夏楚之`。
  - `金圆券` 是金融名词，在《蒋介石的“引退谋和书告”》中只作币制语境。
  - `关键` 是普通词，在陶成章案和肇和起义叙述中作“关键之所系”等语句。
  - `夏楚之` 来自蒋介石信中“先慈夏楚与教导之力”，是古文语句残片。
- 归并别名：`张岳公` -> `张群`。
  - 来源包括《张岳公闲话往事》记录张群口述，以及张岳公作为总统府秘书长语境。
- 正式复核确认：
  - `金圆券`、`关键` 仍是全库其它书的既有节点，但已无本书 evidence / topChapters。
  - `夏楚之`、`张岳公` 已不再作为全库节点存在。
  - 张群本书计数由 34 调整为 36。

新增端点
- 为真假保密局二级关系补入稳定端点：林超、黄逸公、钮殿臣、涂寿眉、郭旭。
- 新增本书短语锚点：
  - `林超为副局长`
  - `派涂寿眉为主任秘书`
  - `黄逸公任处长`
  - `钮殿臣担任`
  - `经理处（处长郭旭）`

关系边复核与补充
- round2 的 45 条主干关系边保持不改，权重层级复核后整体合理：主事件边权重 7-9，资料来源边多为 4-5，名单/内勤边 3-4。
- round3 新增 6 条真假保密局二级边：
  1. 徐志道 -> 林超：公开假保密局/正副局长。
  2. 徐志道 -> 涂寿眉：公开假保密局/主任秘书。
  3. 毛人凤 -> 黄逸公：指定业务处长/拆徐不成。
  4. 徐志道 -> 黄逸公：假局内斗/业务处长倒向徐志道。
  5. 徐志道 -> 钮殿臣：亲戚任总务处长/假局内勤。
  6. 毛人凤 -> 郭旭：保密局经理处/新津机场待机同车。
- 本书关系边：45 -> 51。

单书复验
- 临时单书命令：`scripts/extract_relationships.py --book-path ... --data-dir .tmp-jiangjieshi-xuji-round3-data --export-dir .tmp-jiangjieshi-xuji-round3-exports`
- 单书结果：237 people / 1 book / 20 chapters。
- 单书关系边：51 条。
- 单书确认：`金圆券`、`关键`、`夏楚之`、`张岳公` 均不再出现；林超、黄逸公、钮殿臣、涂寿眉、郭旭进入人物表。

正式重建
- 正式重建方式：读取当前 `data/people.json` 的 113 本书名，逐本映射源码目录并构造重复 `--book-path` 参数。
- 确认：`titles 113 mapped 113 missing 0`。
- 确认：`uses_all_flag: False`。
- 正式结果：12934 people / 113 books / 6666 chapters。
- 总关系边：4053 -> 4059。
- 《蒋介石研究续集》：personCount=284, chapters=20, book_relations=51。
- `data/people.json` size：49,732,410 bytes，仍低于 50MB。

验证
- CSV rows=12934，与 `totals.people` 一致。
- `python -m py_compile scripts/extract_relationships.py` 通过。
- `node --check data/relationship-data.js` 通过。
- `node --check assets/app.js` 通过。
- `git diff --check` 无空白错误；仅有既有 CRLF 提示。

下一轮建议
- 本书可以进入 closeout。
- closeout 建议重点：
  1. 最后扫一遍 `exports/by-book-index.txt` 与 `exports/li-ao-relationships.txt` 中本书切片。
  2. 确认 51 条关系边没有重复、端点都在本书人物表或全库人物表中。
  3. 再查 `data/people.json` 是否仍低于 50MB。
  4. 写本书 closeout note，记录 round1-3 的累计结果与下一本书建议。
