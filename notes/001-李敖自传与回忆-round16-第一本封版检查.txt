《李敖自传与回忆》人物关系挖掘 第十六轮：第一本封版检查

检查定位：
本轮不再扩挖人物，不再开放式校对。只检查第一本书当前成果是否可以作为阶段性封版底稿交给人工校对者使用。

封版数据：
1. 书目：李敖自传与回忆
2. 章节：16
3. 人物：218
4. 身份边：363
5. 重复人物：0
6. 待复核：0
7. 称谓待考：7

保留的称谓待考：
小八、尹女士、小屯、张伯伯、小蕾、勘勘、谌谌。

保留原因：
这些节点在当前文本中确实与李敖关系网有关，但尚无足够证据确认真实姓名。强行合并会制造新错误。后续若其他书提供真实姓名，再通过别名表回填合并。

一致性检查：
1. data/people.json 与 data/relationship-data.js 一致：均为218人、363条身份边。
2. TXT导出为中文可读文本，人物总数为218，适合非程序员人工校对。
3. CSV导出为218行人物记录，和JSON一致。
4. by-book-index.txt 已生成逐本索引，目前只有《李敖自传与回忆》。
5. 网页入口 index.html 正常引用 assets/styles.css、data/relationship-data.js、assets/app.js。
6. 前端分类颜色覆盖全部25个身份分类。
7. 每个保留人物均有证据片段。
8. 未发现重复姓名节点。

验证命令结果：
1. node --check assets/app.js：通过。
2. scripts/extract_relationships.py 语法解析：通过。
3. scripts/__pycache__：不存在。

阶段性结论：
《李敖自传与回忆》进入阶段性封版状态。当前版本可作为第一本书的人物关系图底稿、人工校对底稿和后续跨书合并的基础版本。

封版后处理原则：
1. 不再对第一本做泛泛的“再校对一轮”。
2. 只处理用户指出的具体错人、错边、错分类。
3. 第二本及后续书中若发现第一本称谓的真实姓名，再回填别名合并。
4. 页面和导出格式可以继续优化，但不把这视为第一本数据未完成。

下一步建议：
进入第二本书的人物关系挖掘。若开始第二本，沿用第一本形成的规则：身份节点细分、人物不重复、出现次数保留、低频名单人物保留、神交引用单列、称谓先保留后合并。
