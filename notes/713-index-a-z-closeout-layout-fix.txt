全索引 A-Z closeout 与高密度关系图修复记录

时间：2026-07-10

范围：
- 解决蒋介石、胡适等高连接度人物在关系图中出现人物节点越出画布、关系边形成平行斜线束的问题。
- 对 A-Z 单轮清理后的全部索引执行最终 closeout：核对拼音分桶、分片、身份边、人物关系、逐书计数、站点数据与全部导出。
- 本轮没有使用 `--all`，没有重建 163 本全集，也没有改变已完成的 154 本存档数据。

高密度关系图问题与修复：
- 原布局为自写迭代力导向；当锁定蒋介石时会同时载入 210 位直接相关人物、43 类身份及大量重复关系记录，节点间排斥力持续把人物推出软边界。
- 旧 `fitGraphView` 又设置了较高的最小缩放下限，不能完整容纳已经发散的包围盒，因此画面只剩中心身份节点，人物和边端点落在画布外。
- 改为确定性的分层径向布局：李敖与锁定人物置于中心，身份分类按数量分一至两圈，相关人物按主身份角度排序后分布到多个人物环。
- 去掉无语义的“李敖到所有人物”结构边；非锁定人物在图上只绘制最强主身份边，完整身份仍保留在详情数据中。
- 锁定人物保留全部身份边；关系图只绘制锁定人物与其直接邻居之间的关系，邻居彼此的边不再干扰焦点视图。
- 同一人物对在不同书籍、不同记录中的多条关系聚合为一条曲线；悬停标题汇总关系类型，详情面板仍逐条展示全部原始关系。
- 所有边改为圆周端点到圆周端点的二次曲线，不再穿过节点；左右半区标签分别向外对齐。
- 自动适配缩放取消会导致裁切的最小比例，并将双击复位改为回到当前图的完整适配视图。

浏览器验证：
- 默认全图：269 个节点，882 条可视边，画布外节点 0。
- 蒋介石：210 位相关人物、238 个总节点、212 条身份边、209 条聚合关系曲线，画布外节点 0；初始适配比例约 0.546。
- 胡适：142 位相关人物、171 个总节点、148 条身份边、141 条聚合关系曲线，画布外节点 0；初始适配比例约 0.693。
- 390 x 844 移动端：SVG 实际宽 353.6px，页面 scrollWidth 375 小于 innerWidth 390，无横向溢出；蒋介石视图 238 个节点全部在画布内。
- 精确搜索回归：输入 `陈平景` 后，详情标题和选中节点均为陈平景，图中有陈平景且无高玉树。
- 浏览器控制台无 error/warn。

A-Z 清理汇总：
- 人物总数从索引清理开始前的 16013 降至 11955，共删除 4058 个明确非人物节点。
- 26 次有数据变化的索引记录首尾连续，前一轮结果与后一轮起点全部一致。
- I 组只有 1 人，无需清理；U、V 组为空，因此没有单独清理记录。
- 职衔、关系、某人妻、某先生、匿名具体人物、历史/文学/虚构人物、昵称及仍能确指人物的残缺称谓均按约定保留。

最终拼音分桶（合计 11955）：
- A 125，B 203，C 1083，D 351，E 9，F 245，G 410，H 887，I 1。
- J 390，K 154，L 1990，M 362，N 87，O 27，P 176，Q 239，R 64。
- S 722，T 270，U 0，V 0，W 1195，X 687，Y 891，Z 1386，# 1。
- 27 个分桶中 25 个非空；按网页同一套 `Intl.Collator('zh-CN-u-co-pinyin')` 边界计算，桶总数与人物总数完全一致。

最终数据状态：
- people = 11955
- identityLinks = 31356
- personRelations = 9128
- books = 154
- chapters = 7921
- categoryCount = 68
- shards = 6

全局一致性校验：
- manifest people = loaded people = shard people = totals people = site people = CSV people = 11955
- 人物姓名无重复；人物分类、出现次数、书数和章节数结构有效。
- 分片 part/count/bytes/generatedAt 全部正确，badShards = 0。
- identityLinks 无悬空端点、无重复人物/分类键，且与每个人物的 categoryStats 完全一致。
- personRelations 无悬空端点，badRelations = 0。
- 所有逐书展示人物均存在，personCount 不小于展示行数，badBooks = 0。
- categoryCounts 与剩余人物重新计算结果一致。
- `data/relationship-data.js` 与完整存档的 compact site data 完全一致。
- CSV 人物顺序、TXT 人物集合、by-book 全文均与存档完全一致。
- 现存人物与 `STOP_NAMES` 仅交叉到受保护的文学人物化名称 `大风`；未受保护的停止词残留为 0。
- `node --check assets/app.js`
- `node --check data/relationship-data.js`
- `python -m py_compile scripts/extract_relationships.py`
- `git diff --check` 通过，仅有工作区既有的 LF/CRLF 提示。

结论：
- A-Z 主索引单轮清理和全局 closeout 已完成。
- 当前无需逐字母开启第二轮；后续若继续优化，建议转入人物合并/别名归一和关系类型聚合，而不是继续按字母删除。
