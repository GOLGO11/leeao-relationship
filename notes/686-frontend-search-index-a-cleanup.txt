# 前端搜索与 A 组人名清理

## 范围
- 目标：优化静态前端搜索/索引体验，并从 A-Z 索引的 A 组开始清理明显不是人名的残片。
- 本次不做源书扫描、不做正式抽取重建，也没有使用 `--all`。

## 前端
- 移除页面底部“逐本挖掘进度”区域及相关 `bookGrid/bookCount/renderBooks` 逻辑。
- 保留统计区中的本书总数，用于展示当前数据覆盖规模。
- 搜索逻辑已在上一轮改为姓名/别名优先，A-Z 人名索引仍保留。

## A 组清理名单
- 删除 20 个非人物节点：`AB座邻居（李敖书启集）`、`安之意`、`安保`、`安全性`、`安全考`、`安去选`、`安太岁`、`安妇老`、`安妇血`、`安家费`、`安心做`、`安息`、`安抚国`、`安排李`、`安礼`、`安维持`、`安蒙难`、`安街`、`安郡王`、`敖之说`。
- 同步删除 1 条相关人物关系边：`AB座邻居（李敖书启集） -- AB座会议/代投票委托 -- 张牙医（李敖书启集）`。
- 保留证据明确的人名或人物化称谓，如 `安良`、`安秉仁`、`安健民`、`安娜·赫尔德`、`安坑监狱政工官（李敖书札集）` 等，留待后续更细的人工判断。

## 数据同步
- `scripts/extract_relationships.py`：新增 A 组清理名单到全局 `STOP_NAMES`，防止未来重跑时回流。
- `data/people.json` 与分片：总人数 `16013 -> 15993`。
- `identityLinks`：`37307 -> 37283`。
- `personRelations`：`9136 -> 9135`。
- `exports/li-ao-relationships.csv`：行数同步为 15993。
- A 组当前人数：`145 -> 125`。

## 校验
- `node --check assets/app.js`：通过。
- `node --check data/relationship-data.js`：通过。
- `python -m py_compile scripts/extract_relationships.py`：通过。
- `git diff --check`：通过；仅有 LF/CRLF 提示。
